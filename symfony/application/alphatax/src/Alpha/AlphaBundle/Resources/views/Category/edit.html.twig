{% extends '::base.html.twig' %}
{% form_theme edit_form 'form_table_layout.html.twig' %}

{% block body -%}
    <h1>Category edit</h1>
{#
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('category') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
  #}  
    
    
    <div class="container" >
        {#
        <form action="{{ path('category_update')}}" method="post" {{ form_enctype(form) }} class="form-update-category">
        #}
        {#
        {{ form_row( form.name ) }}
        <br>
        {{ form_row( form.description ) }}
        #}
        
        {{ form(edit_form) }}
        
        <div>
        <a class="btn btn-xs btn-success" href="{{ path('category') }}">
            Back to the list
        </a>
        {{ form(delete_form) }}    
        </div>
        {#
        </form>
        #}
    </div>
{% endblock %}
